/* import { TestBed, inject } from '@angular/core/testing';

import { AccountLanguageService } from './account-language.service';

describe('AccountLanguageService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AccountLanguageService]
    });
  });

  it('should be created', inject([AccountLanguageService], (service: AccountLanguageService) => {
    expect(service).toBeTruthy();
  }));
});
 */