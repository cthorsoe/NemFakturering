/* import { TestBed, inject } from '@angular/core/testing';

import { DashboardLanguageService } from './dashboard-language.service';

describe('DashboardLanguageService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DashboardLanguageService]
    });
  });

  it('should be created', inject([DashboardLanguageService], (service: DashboardLanguageService) => {
    expect(service).toBeTruthy();
  }));
});
 */