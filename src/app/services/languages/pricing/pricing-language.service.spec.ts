/* import { TestBed, inject } from '@angular/core/testing';

import { PricingLanguageService } from './pricing-language.service';

describe('PricingLanguageService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PricingLanguageService]
    });
  });

  it('should be created', inject([PricingLanguageService], (service: PricingLanguageService) => {
    expect(service).toBeTruthy();
  }));
});
 */