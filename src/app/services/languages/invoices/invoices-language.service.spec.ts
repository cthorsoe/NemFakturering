/* import { TestBed, inject } from '@angular/core/testing';

import { InvoicesLanguageService } from './invoices-language.service';

describe('InvoicesLanguageService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [InvoicesLanguageService]
    });
  });

  it('should be created', inject([InvoicesLanguageService], (service: InvoicesLanguageService) => {
    expect(service).toBeTruthy();
  }));
});
 */